#!/bin/bash

chmod +x

<переменная>=<значение>	#записать значение в переменную

${<переменная>}	#получить значение из переменной

'$var'	#текст
"$var"	#значение переменной

./script.sh arg1 arg2 arg3
	$1	#первый аргумент
	$2	#второй аргумент
	...
	$0	#имя скрипта
	$#	#количество аргументов

if [[ условие ]]
then
	#действия
elif [[ условие ]]
then
	#действия
else
	#действия
fi
условия числа (строки):
	-z <строка>	#строка пустая
	-n <строка>	#строка не пустая
	-eq ==
	-ne !=
	-lt <
	-le <=
	-gt >
	-ge >=
условия (файла):
	-e <путь>	#путь существует
	-f <путь>	#это файл
	-d <путь>	#это директория
	-s <путь>	#размер файла > 0
	-x <путь>	#файл исполняемый
условия (логические):
	!	#НЕ
	&&	#И
	||	#ИЛИ

case <переменная> in
<знач1>)
	#действия
	;;
<знач2>)
	#действия
	;;
*)
	#действия
esac

for <переменная> in <список значений> (знач1 знач2 знач3)
do
	#действия
	break		#прервать выполнение
	continue	#перейти на след. значение
done

while [[ условие ]]
do
	#действия
	read <переменная>	#записать значение в переменную-т
	read -p "текст" var1 var2
done

арифметика
let "переменная = выражение"
	+ - * /
	%
	**

внешние программы
переменная=`программа`
	$?	#узнать код возврата
		0	#нормальное завершение
		>0	#завершено с ошибкой
	exit <код>	#выйти с кодом <код>

имя_функции ()
{
	var_global=1
	local var_local=1
	#действия с $1, $2, ... , $#
}

имя_функции () { действ1; действ2; }

имя_функции арг1 арг2 ...


sed 's/John/Nick/g' old.txt > new.txt
	#заменить все John на Nick
sed -r 's/J[a-z]*n/Nick/g' old.txt > new.txt
	#заменить все слова, которые начинаются на J и заканчиваются на n на Nick
sed -n '2,4p' file.txt
	#вывести строки со 2 по 4
sed '2,4d' file.txt
	#вывести все строки кроме 2-4
sed -n '/[0-9]\{2\}/p' file.txt
	#вывести строки с 2 цифрами подряд
sed '2,/[Rr]ight/d' file.txt
	#вывести все строки кроме со 2 до строки содержащей "right" или "Right"
sed 's/ [A-Z]\{2,\} / abbreviation /g' file.txt
	#заменить аббревиатуры от 2 латинских букв окруженные пробелами на слово "abbreviation"

wc -[lcmLw] <filename>
	-l	#вывести количество строк
	-c	#вывести количество байт
	-m	#вывести количество символов
	-L	#вывести длину самой длинной строки
	-w	#вывести количество слов